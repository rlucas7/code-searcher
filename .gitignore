# standard flask gitignore
.venv/

*.pyc
__pycache__/

instance/

.pytest_cache/
.coverage
htmlcov/

dist/
build/
*.egg-info/


# added stuff
*bin*
*lib*
*var*
*share*
pyvenv.cfg

# a small sample of this file is committed to repo so that it is self
# contained but it is not the entire repo embedded
collections-c-embeds.jsonl

# a shell file to store env vars e.g. OPEN_AI_API_KEY, etc...
envvars.sh

# generated files when executing the workflow
*.csv
*.csv